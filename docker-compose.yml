version: '3.8'

services:
  redis:
    image: redis
  db:
    image: postgres
    expose:
      - 5432
  celery:
    build:
      context: ./backend
    depends_on:
      - redis
      - db
    command: celery -A app.celery worker
  backend:
    build:
      context: ./backend
    depends_on:
      - redis
      - celery
      - db
  frontend:
    build: 
      context: ./frontend
    depends_on:
      - redis
      - celery
      - backend
      - db

